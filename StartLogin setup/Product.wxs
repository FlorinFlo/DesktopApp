<?xml version="1.0" encoding="UTF-8"?>
<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi">
	<Product Id="*" Name="StartLogin_setup" Language="1033" Version="1.0.0.0" Manufacturer="Microsoft" UpgradeCode="9042644a-4d0a-4aed-9f14-86d0debde011">
		<Package InstallerVersion="200" Compressed="yes" InstallScope="perMachine" />

		<MajorUpgrade DowngradeErrorMessage="A newer version of [ProductName] is already installed." />
		<MediaTemplate  EmbedCab="yes"/>

		<Feature Id="ProductFeature" Title="StartLogin_setup" Level="1">
			<ComponentGroupRef Id="ProductComponents" />
		</Feature>
    <Property Id="WIXUI_INSTALLDIR" Value="INSTALLFOLDER" />
    <UI>
      <UIRef Id="WixUI_Mondo"/>
      <UIRef Id="WixUI_ErrorProgressText"/>
      
      <Publish Dialog="LicenseAgreementDlg" Control="Next" Event="NewDialog" Value="Info" Order="10" >LicenseAccepted = "1"</Publish>
      <Publish Dialog="Info" Control="Next" Event="NewDialog" Value="CustomizeDlg" Order="10" >1</Publish>
      <Publish Dialog="CustomizeDlg" Control="Back" Event="NewDialog" Value="Info" Order="10">NOT Installed</Publish> 
      
    </UI>
    <InstallExecuteSequence>
      <Custom Action="CustomAction1" Before ="InstallFinalize">NOT Installed</Custom>
    </InstallExecuteSequence>
	</Product>

	<Fragment>
		<Directory Id="TARGETDIR" Name="SourceDir">
			<Directory Id="ProgramFilesFolder">
				<Directory Id="INSTALLFOLDER" Name="Login_setup" />
			</Directory>
		</Directory>
	</Fragment>

	<Fragment>
		<ComponentGroup Id="ProductComponents" Directory="INSTALLFOLDER">
			<!-- TODO: Remove the comments around this Component element and the ComponentRef below in order to add resources to this installer. -->
      <Component Id="ProductComponent">
        <File Id="StartLogIn.exe" Source="$(var.StartLogIn.TargetPath)" KeyPath="yes"></File>
      </Component>
				<!-- TODO: Insert files, registry keys, and other resources here. -->
			<!-- </Component> -->
		</ComponentGroup>
	</Fragment>
</Wix>